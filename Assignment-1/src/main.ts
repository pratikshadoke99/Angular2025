import { bootstrapApplication } from '@angular/platform-browser';
import { appConfig } from './app/app.config';
import { App } from './app/app';

bootstrapApplication(App, appConfig)
  .catch((err) => console.error(err));



  //Java
  //module ( package, namespace)
  //logical collection of java types
  // class, interface

  //C#
  //module ( namespace)
  //logical collection types of C#
  //class, interface, enum, struct, delegate, events
  
  //Typescript
  //Module
  //logical colletion angular types
  //class, interface

  //Typescript along with Angular
  //Module
  //Angualr Modules
  //logical collection of Angular types
  // components, interface, Injectables, directives, pipes,
  // interceptors, events, 
  
